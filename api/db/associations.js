/*
Helper function to define associations between models
*/

async function applyAssociations(sequelize) {
    const {
        contract_records,
    } = sequelize.models;
  }
  
  module.exports = {applyAssociations};